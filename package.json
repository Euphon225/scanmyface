{
  "name": "fc26",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "func start",
    "test": "echo \"No tests yet...\"",
    "start:doppler:dev": "doppler run -c dev -- func start",
    "start:doppler:staging": "doppler run -c staging -- func start",
    "deploy:functions:staging": "doppler run -c staging -- func azure functionapp publish scanmyface-engine-staging",
    "deploy:functions:production": "doppler run -c production -- func azure functionapp publish scanmyface-engine",
    "build:frontend": "echo \"Frontend built (no bundler configured)\"",
    "deploy:frontend": "git push origin main"
  },
  "dependencies": {
    "@azure/functions": "^4.0.0",
    "mongodb": "^7.2.0",
    "stripe": "^16.12.0"
  },
  "main": "src/{index.js,functions/*.js}"
}
